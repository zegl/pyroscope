---
log-level: debug
scrape-configs:
  - job-name: testing
    enabled-profiles: [cpu, mem, goroutines, mutex, block]
    file-sd-configs:
      - files:
         - /etc/pyroscope/my-target-group.yml
        refresh-interval: 10s
